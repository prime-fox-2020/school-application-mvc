<html>
    <head>
        <title>subjects</title>
        <style>
            table, th, td {
              border: 1px solid black;
              border-collapse: collapse;
            }
            th, td {
              padding: 5px;
              text-align: left;    
            }
        </style>
    </head>
    <body>
        <h1 style="text-align: center;">Data Subjects</h1>
        <table>
            <tr>
                <th>id</th>
                <th>subject_name</th>
            </tr>
            <% for(let i=0; i<data.length; i++) { %>
                <tr>
                    <td><%= data[i].id %></td>
                    <td><%= data[i].subject_name %></td>
                </tr>           
            <% } %>
        </table>
    </body>
</html>