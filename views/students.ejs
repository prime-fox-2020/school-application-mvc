<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Students Data | SMP Negeri 1 Tangerang</title>
  </head>
  <body>
    <h1>Students Data</h1>
    <table border="1" style="width:100%; border: 1px solid black;
    border-collapse: collapse;">
      <tr>
        <th>ID</th>
        <th>Firstname</th>
        <th>Lastname</th>
        <th>Email</th>
        <th>Options</th>
      </tr>
      <% for(let i = 0; i < students.length; i++) { %> 
      <tr>
        <td><%= students[i].id %></td>
        <td><%= students[i].first_name %></td>
        <td><%= students[i].last_name %></td>
        <td><%= students[i].email %></td>
        <td><a href="/students/<%= students[i].id %>/edit">Edit</a> | <a href="/students/<%= students[i].id %>/delete">Delete</a></td>
      </tr>
      <% } %> 
    </table> 
    <a href="/students/add"><h3>Add student</h3></a>
  </body>
</html>