<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>Students List</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet' type='text/css' media='screen' href='main.css'>
    <script src='main.js'></script>
    <style>
        table, td {
        border: 1px solid black;
        }
    </style>
</head>
<body>
    <h1>Students List</h1>
    <table>
        <tr>
            <th>ID</th>
            <th>first_name</th>
            <th>last_name</th>
            <th>email</th>
            <th>gender</th>
            <th colspan="2">birthdate</th>
        </tr>
    <% for(let a = 0; a < data.length; a++){%>
        <% if(data[a].email == email){ %>
            <tr>
                <td><%= data[a].id %></td>
                <td><%= data[a].first_name %></td>
                <td><%= data[a].last_name %></td>
                <td><%= data[a].email %></td>
                <td><%= data[a].gender %></td>
                <td><%= data[a].birthdate %></td>
                <td><a href="/students/<%=data[a].id%>/edit">Edit</a></td>
                <td><a href="/students/<%=data[a].id%>/delete">Delete</a></td>
            </tr>
        <% } else if(email === undefined){ %>
            <tr>
                <td><%= data[a].id %></td>
                <td><%= data[a].first_name %></td>
                <td><%= data[a].last_name %></td>
                <td><%= data[a].email %></td>
                <td><%= data[a].gender %></td>
                <td><%= data[a].birthdate %></td>
                <td><a href="/students/<%=data[a].id%>/edit">Edit</a></td>
                <td><a href="/students/<%=data[a].id%>/delete">Delete</a></td>
            </tr>
        <% }%>
    <% }%>
    </table>

    <a href="/students/add">Add New Student</a>
    <a href="/">Halaman Utama</a>
    
</body>
</html>