<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>subjects</title>
</head>
<body>
    <h1>Data Subjects</h1>
    <button><a href="/subjects/add">Add Subjects</a></button><br><br>
    <table border="1">
        <thead>
            <tr>
                <td>ID</td>
                <td>SUBJECT NAME</td>
                <td colspan="2">OPTIONS</td>
            </tr>
        </thead>
        <tbody>
            <%if(data) {%>
                <%for(let i = 0; i < data.length; i++) {%>
                    <tr>
                        <td><%= data[i].id %></td>
                        <td><%= data[i].subject_name %></td>
                        <td><button><a href="/subjects/<%= data[i].id %>/edit">Edit</a></button></td>
                        <td><button><a href="/subjects/<%= data[i].id %>/delete">Delete</a></button></td>
                    </tr>
                <%}%>
            <%}%>
        </tbody>
    </table>
    
    <br><br>
    <button><a href="/">Back to Home</a></button>
</body>
</html>
